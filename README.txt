train.bat 

--img size of image (must be square)
--batch number of batches
--epochs number of epochs
--data points to data.yaml file
--cfg points to yolo model

live.bat

--source 0 is webcam
--weights points to weights, update if doing more training